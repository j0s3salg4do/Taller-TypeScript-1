{
    "compilerOptions": {
      "target": "es6",                       // Establece la versión de ECMAScript a la que deseas compilar
      "module": "commonjs",                  // Especifica el sistema de módulos
      "outDir": "./dist",                    // Carpeta de salida para los archivos compilados
      "rootDir": "./",                        // Carpeta raíz del proyecto
      "strict": true,                         // Habilita todas las comprobaciones de tipo
      "esModuleInterop": true,                // Permite la interop de módulos ES
      "skipLibCheck": true,                   // Omite la comprobación de tipos de archivos de declaración de bibliotecas
      "forceConsistentCasingInFileNames": true // Asegura que el uso de mayúsculas y minúsculas en los nombres de archivo sea consistente
    },
    "include": [
      "*.ts"                                  // Incluye todos los archivos .ts en la carpeta base
    ],
    "exclude": [
      "node_modules"                          // Excluye la carpeta node_modules
    ]
  }
  